#include <bits/stdc++.h>

using namespace std;

typedef long long lint;

struct Elephant {
  lint weight,
};

int main() {
  int n;
  scanf("%d", &n);

  for (int i = 0; i < n; i++) {
    
  }

  return 0;
}